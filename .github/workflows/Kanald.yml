name: Kanal D Güncelleyici

on:
  schedule:
    - cron: '0 4 * * *'  # Her gün 04:00 UTC
  workflow_dispatch:      # Manuel tetikleme butonu

permissions:
  contents: write

jobs:
  scrape_and_update:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Repoyu Çek
        uses: actions/checkout@v3

      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 tqdm

      - name: 📂 Klasörü Hazırla
        run: mkdir -p KanalD

      - name: 🚀 Scriptleri Çalıştır
        run: |
          echo "Diziler ve Programlar taranıyor..."
          python kanald_series.py
          
          echo "Filmler taranıyor..."
          python kanald_movies.py

      - name: 💾 Değişiklikleri Kaydet (Push)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Sadece 'KanalD' klasörünü ekle
          git add KanalD
          
          # Eğer değişiklik varsa commit at
          git commit -m "Kanal D Listesi Güncellendi" || echo "Değişiklik bulunamadı."
          
          git push
