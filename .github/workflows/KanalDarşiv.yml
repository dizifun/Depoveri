name: Kanal D Full Arşiv Güncelleyici

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape_and_archive:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Repoyu Çek (Tam Geçmiş)
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 tqdm

      - name: 📂 Klasör Kontrolü
        run: mkdir -p KanalD_Arsiv

      - name: 🚀 Scripti Çalıştır
        run: |
          # Dosya adının birebir aynı olduğundan emin ol
          python kanald_full_scraper.py

      - name: 💾 Kaydet ve Yükle (Senkronizasyon Korumalı)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Arşiv klasörünü ekle
          git add KanalD_Arsiv
          
          # Eğer değişiklik yoksa hata verme, varsa yükle
          if git diff --staged --quiet; then
            echo "✅ Değişiklik yok. Her şey güncel."
          else
            git commit -m "Otomatik Güncelleme: Kanal D Arşivi"
            
            # --- İŞTE ÇÖZÜM BURADA ---
            # Önce sendeki son değişiklikleri çeker, sonra yenileri ekler
            git pull --rebase origin main
            
            # Şimdi hatasız yükler
            git push origin main
          fi
