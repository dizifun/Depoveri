name: Kanal D Full Arşiv Güncelleyici

on:
  schedule:
    # Her gün sabah 06:00'da (Türkiye saatiyle yaklaşık 09:00) çalışır
    - cron: '0 6 * * *'
  workflow_dispatch: # Manuel çalıştırma butonu

permissions:
  contents: write

jobs:
  scrape_and_archive:
    runs-on: ubuntu-latest

    steps:
      # 1. ADIM: Repoyu Tam Geçmişiyle Çek (fetch-depth: 0 çok önemli)
      - name: 📥 Repoyu Çek (Checkout)
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      # 2. ADIM: Python Kurulumu
      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. ADIM: Kütüphaneleri Yükle
      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 tqdm

      # 4. ADIM: Klasör Kontrolü
      - name: 📂 Klasör Yapısını Doğrula
        run: mkdir -p KanalD_Arsiv

      # 5. ADIM: Scripti Çalıştır
      - name: 🚀 Kanal D Tarayıcıyı Çalıştır
        run: |
          # Dosya adının repodakiyle BİREBİR aynı olduğundan emin ol
          python kanald_full_scraper.py

      # 6. ADIM: Dosyaları Listele (Kontrol Amaçlı)
      - name: 🔍 Oluşan Dosyaları Listele
        run: ls -R KanalD_Arsiv/

      # 7. ADIM: Hata Veren "Push" Kısmının Düzeltilmiş Hali
      - name: 💾 Sonuçları Kaydet ve Yükle (Fix: Rebase)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # Arşiv klasörünü ekle
          git add KanalD_Arsiv
          
          # Değişiklik var mı kontrol et
          if git diff --staged --quiet; then
            echo "⚠️ Değişiklik yok, commit atlanıyor."
          else
            git commit -m "Kanal D Güncellemesi: Otomatik Arşiv"
            
            # --- KRİTİK DÜZELTME BURADA ---
            # GitHub'daki son değişiklikleri çekip bizimkilerin altına koyar (Rebase)
            # Bu satır "fetch first" hatasını çözer.
            git pull --rebase origin main
            
            # Şimdi güvenle yükle
            git push origin main
          fi
